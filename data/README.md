# Dados

O arquivo `top_50.csv` é a amostra fornecida com o projeto. Ele usa UTF-16 e
contém apenas a classe `normal`, portanto não é suficiente para treinamento.

Adicione nesta pasta uma amostra do ERENO com registros normais e ataques e
informe o nome do arquivo ao executar `main.py`.
